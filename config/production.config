#!coffee

base_uri: "http://documentup.com"
redis:
  port: require('url').parse(process.env.REDIS_URL).port
  hostname: require('url').parse(process.env.REDIS_URL).hostname
server:
  port: process.env.PORT || 5000
